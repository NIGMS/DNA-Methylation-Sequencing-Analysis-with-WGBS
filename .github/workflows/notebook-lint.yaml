name: 'Lint Notebook'
on:
  push:
permissions:
  contents: write
  id-token: write

jobs:
  lint:
    name: 'Linting'
    uses: NIGMS/NIGMS-Sandbox-Repository-Template/.github/workflows/notebook-lint.yaml@main
    with:
      directory: .
